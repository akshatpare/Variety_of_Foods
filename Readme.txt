Readme:

>> First we added the folder in vs code then we addded the files index.html, style.css, script.js.

>> Then we will link the cdn link of font-awesome from the font-awesome libraries page. copy the cdn link>open link tab in head section> paste the copied link.

>> Then we will open the live server.

>> Then we will create a div with the class ["Container"].
 

Note: Select (*) in css to select every element.

Note: We will use rem as the unit in entire project 1rem = 16px.

Note: We can convert 1rem = 10 px.

Note: The Screen Size is 1920 1080.